1. Systematic HPO Customizations per ModelThe transition from the baseline to the tuned stage involved moving from default parameters to a targeted Randomized Search (100 iterations) with 5-Fold Cross-Validation, specifically minimizing RMSLE.A. LightGBM (The Accuracy Champion)Dedicated Change: Switched to a Leaf-wise growth strategy (using num_leaves) rather than level-wise.Tuning Focus: Optimized the learning_rate (0.01 to 0.3) and feature_fraction (0.6 to 1.0) to prevent overfitting on specific climate features.Early Stopping: Integrated a custom monitor split (10%) to halt training when the validation loss plateaued, ensuring the model didn't "memorize" the 2022-2024 noise.B. CatBoost (Categorical Specialist)Dedicated Change: Optimized for Symmetric Trees, which allows for much faster inference.Tuning Focus: Heavily explored l2_leaf_reg (regularization) and random_strength to improve the model's ability to handle the division-wise categorical data without bias.Performance Tweak: Increased early_stopping_rounds to 100 to allow the model more patience during training on complex monsoon patterns.C. XGBoost (Boosting Baseline)Dedicated Change: Used a DMatrix-based training wrapper to enable low-level early stopping.Tuning Focus: Focused on min_child_weight (1 to 10) and gamma (0 to 0.5) to control the conservative nature of tree splits, making the model more robust to sudden dengue peaks.D. Random Forest (Bagging Baseline)Dedicated Change: Focused on Feature Sub-sampling (max_features) to ensure the model uses diverse climate indicators (humidity vs. rainfall) for each tree.Tuning Focus: Optimized min_samples_leaf and n_estimators (up to 500) to maximize the "wisdom of the crowd" effect, which significantly reduced the RMSE compared to the single-tree baseline.E. Support Vector Regression (SVR)Dedicated Change: Focused strictly on the Radial Basis Function (RBF) kernel.Tuning Focus: Logarithmic search for C (0.1 to 1000) and gamma (scale vs. auto).New Addition: Introduced epsilon tuning (0.01 to 0.5) to define the "error-free zone" around the regression line, which is critical for stabilizing predictions on zero-patient days.F. Ridge Regression (Linear Baseline)Dedicated Change: Switched from a fixed alpha to a log-uniform search for the alpha parameter (regularization strength).Tuning Focus: Explored values from $10^{-4}$ to $10^{4}$ to find the exact point where the linear model stops overfitting to temporal noise while maintaining the core climate trends.2. The "Refit & Re-time" StrategyA major dedicated change in your workflow is the Final Training Duration calculation. Unlike the baseline (where training time was measured during a single fit), the HPO stage includes:Refit=True: The RandomizedSearchCV automatically re-trains the "Best Estimator" on the full 100% training set (2022–2024) once the best parameters are found.Dedicated Timing Script (re_time_hpo_models.py): You created a script to specifically measure the final fit time for the tuned models. This ensures the "Training Time (s)" reported in Table II is the most scientifically accurate representation of the optimized model's complexity.3. Summary Table: From Baseline to Dedicated HPOFeatureBaseline StageDedicated HPO StageParametersDefaults (Standard)Optimized (Randomized Search)Model ScopeStandard Scikit-Learn fitEarly Stopping (LGBM, CatBoost, XGB)Data SplitSingle Train/Test Split5-Fold Cross-Validation (K-Fold)Objective$R^2$ / MSERMSLE (Log-space optimization)TimingSingle Fit MeasureDedicated Fit Re-timing for accuracy